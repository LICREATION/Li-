# 术语速查（查字典用的，不是背的）

> **怎么用这张表**：读代码或论文时遇到不懂的词，在这里**加一行**，写「在哪遇到的 + 你的一句话理解」。
>
> **不要从头背它。** 今天背的词，两周不遇就忘；在真实代码里撞见三次的词，不用背也会。
> 这张表唯一的用处是：**让你知道自己不懂的那个东西叫什么，好去查。**
>
> 专业名词永远学不完。判断你是不是真的懂了，只有一个标准：**能不能用一句话给外行讲明白。**

---

## Git / GitHub（每天都会撞见）

| 词 | 一句话 |
|---|---|
| repository / repo | 一个项目的文件夹 + 它全部的修改历史 |
| commit | 一次「存档」，记录这一刻所有文件的状态 |
| add / stage | 把改动标记为「这次要存进去的内容」 |
| push | 把本地存档上传到 GitHub |
| pull | 把 GitHub 上的新内容拉回本地 |
| remote / origin | 远端仓库地址的别名，origin 是默认名 |
| branch / main | 分支；main 是主分支，相当于正式版本 |
| clone | 把远端仓库完整下载到本地 |
| README.md | 仓库首页说明，别人点进来第一眼看到的东西 |
| .gitignore | 声明哪些文件不上传（数据、密钥、缓存） |
| requirements.txt | 依赖清单，别人照它能装出一模一样的环境 |
| fork / PR | 复制别人的仓库 / 向别人的仓库提交修改建议 |
| contribution graph | GitHub 首页那片绿格子，每次提交点亮一格 |

## Python / 数据处理

| 词 | 一句话 |
|---|---|
| pandas | Python 里处理表格的库 |
| DataFrame | pandas 的表格对象，有行索引和列名 |
| numpy | 数值计算库，核心是数组 |
| 向量化 | 对整个数组一次性运算，而不是 for 循环逐个算（快 10–100 倍） |
| akshare | 免费中文金融数据接口，A股行情/财务/宏观都能取 |
| 日线 / 月频 | 每个交易日一行数据 / 每个月一行数据 |
| 面板数据 panel | 同一批股票在多个时间点上的数据（股票 × 时间） |
| resample | 把日线聚合成周线/月线 |
| 缺失值 NaN | 数据空缺，必须先处理，否则模型直接报错 |
| z-score 标准化 | (x − 均值) ÷ 标准差，把不同量纲的变量拉到可比 |
| 截尾 winsorize | 把极端值压到 1%/99% 分位，防止个别异常值主导结果 |

## 金融

| 词 | 一句话 |
|---|---|
| 收益率 | （今天价 − 昨天价）÷ 昨天价 |
| 横截面 cross-section | 同一时点上，所有股票之间的横向比较 |
| 时间序列 time series | 同一只股票，随时间的变化 |
| 因子 factor | 用来解释或预测收益的那个变量（市值、换手率、Beta……） |
| Beta | 个股相对大盘的敏感度，>1 表示波动比大盘更大 |
| 波动率 | 收益率的标准差，衡量风险 |
| 无风险利率 | 通常用国债或 Shibor；超额收益 = 收益 − 无风险利率 |
| 多空组合 | 买预期最好的一组、卖最差的一组，赚的是价差 |
| 十分位 decile | 按预测值把股票分成 10 组，比较第 1 组和第 10 组 |
| 最大回撤 | 从最高点到之后最低点的最大跌幅 |
| 夏普比率 | 每承担一单位风险换来的超额收益 |
| 滑点 | 实际成交价与预期价的差，回测必须计入 |
| T+1 | A股当天买不能当天卖 |
| 佣金 | 交易手续费，通常万三 |
| 样本内 / 样本外 | 训练用的那段数据 / 完全没参与训练的那段数据 |
| 未来函数 | 不小心用了「当时还不可能知道」的信息，回测结果虚高 |
| 过拟合 | 模型把噪声当规律记住了，样本内很漂亮、样本外崩盘 |

## 机器学习 / 计量

| 词 | 一句话 |
|---|---|
| 特征 feature | 模型的输入变量（在金融里就是因子） |
| 标签 label | 模型要预测的目标（如下月收益） |
| 训练集 / 测试集 | 用来拟合模型的数据 / 用来检验效果的数据 |
| 随机切分 vs 时序切分 | 随机打乱会让未来数据漏进训练（金融大忌）→ 必须按时序切分 |
| 交叉验证 | 把数据切成几份轮流做训练与测试，用来调参 |
| 超参数 | 模型外部由人设定的参数（如树的数量、惩罚强度） |
| 网格搜索 GridSearchCV | 穷举所有超参数组合，挑表现最好的 |
| 随机森林 | 很多棵决策树投票表决，比单棵树稳 |
| Lasso / Elastic Net | 带惩罚项的线性回归，能自动把没用的特征筛掉 |
| R² | 模型解释了多大比例的波动；**资产定价里 0.5% 就已经很强** |
| IC | 预测值与真实收益的相关系数，衡量因子有没有用 |
| OLS | 普通最小二乘回归，最基础的线性回归 |
| 残差 | 实际值 − 预测值 |
| p 值 / 显著性 | 结果由巧合造成的概率有多小，< 0.05 通常算显著 |
| 内生性 | 自变量和误差项相关，导致估计不可信（计量的核心难题） |
| 特征重要性 | 模型认为哪个输入变量贡献最大 |

---

## 我还没搞懂的（遇到一个加一行）

| 术语 | 在哪遇到的 | 我的一句话理解 |
|---|---|---|
| | | |
